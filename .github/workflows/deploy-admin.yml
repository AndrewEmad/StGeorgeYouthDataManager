name: Deploy AdminApp to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: deploy-admin
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 5.FrontEnd/AdminApp/package-lock.json

      - name: Install dependencies
        working-directory: 5.FrontEnd/AdminApp
        run: npm ci

      - name: Build AdminApp
        working-directory: 5.FrontEnd/AdminApp
        run: npx ng build --configuration production --base-href /${{ github.event.repository.name }}/admin/

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: 5.FrontEnd/AdminApp/dist/AdminApp/browser
          destination_dir: admin
          keep_files: true
